{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "my-plugin.index",
      "config": {
        "policies": ["global::isAuthenticated"],
        "prefix": false
      }
    }
  ]
}
